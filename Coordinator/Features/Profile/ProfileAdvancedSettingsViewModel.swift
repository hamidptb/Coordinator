import Foundation

class ProfileAdvancedSettingsViewModel {
    private weak var coordinator: ProfileCoordinator?
    
    init(coordinator: ProfileCoordinator) {
        self.coordinator = coordinator
    }
} 